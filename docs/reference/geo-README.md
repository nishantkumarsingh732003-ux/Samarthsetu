# Geo reference data

- `states.json`, `districts.json` — LGD codes for state/district normalization
- `pincode_centroids.csv` — pincode → lat/lon, used when a user gives only a pincode

Loaded into PostGIS by `scripts/seed_data.py`.
